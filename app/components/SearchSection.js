'use client';

import SearchBox from './ui/SearchBox/SearchBox';

export default function SearchSection() {
  return (
    <div className="mb-12 bg-gradient-to-r from-blue-50 to-purple-50 rounded-lg p-8 border border-blue-100">
      <SearchBox />
    </div>
  );
} 